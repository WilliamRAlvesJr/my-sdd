---
description: Desenha o diagrama de classes da feature — o que entra, o que sai e o que muda, com a marca de diff em cima do UML.
argument-hint: "[<feature | caminho da spec>]"
---

Invoque a skill **plan** (`skills/plan/SKILL.md`), com `$ARGUMENTS` como o pedido.
